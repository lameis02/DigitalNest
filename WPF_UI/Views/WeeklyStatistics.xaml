<UserControl x:Class="WPF.Views.WeeklyStatistics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WPF.ViewModel"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="900"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:WeeklyStatisticsVM/>
    </UserControl.DataContext>

    <Grid>

        <Border>
            <Border.Background>
                <ImageBrush ImageSource="/Assets/WaldBild.jpg"/>
            </Border.Background>
        </Border>

        <TextBlock Text="Wochenübersicht"
                   Foreground="#DBDBDB"
                   FontSize="60"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"/>

        <lvc:PieChart LegendLocation="Bottom" Series="{Binding BirdPieSeriesWeekly}" Margin="0,133,450,71" />

    </Grid>
</UserControl>
